# Rust / Cargo build artefacts
target/
**/target/

# Rust dep-graph lockfile is checked in (Cargo.toml ref). Keep Cargo.lock.

# Noir circuit build outputs (regenerated by scripts/hello_proof.sh)
circuits/**/target/

# Stellar runtime artefacts (regenerated by every hello_proof.sh run)
.contract_id

# Editor / OS
.DS_Store
*.swp
.vscode/
.idea/
